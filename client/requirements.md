## Packages
framer-motion | Smooth animations for lists and page transitions
date-fns | Formatting timestamps (e.g., "2 minutes ago")
lucide-react | Icons for the dashboard (already in base, but listing for clarity)
clsx | Utility for conditional classes (already in base)
tailwind-merge | Utility for merging tailwind classes (already in base)

## Notes
Tailwind Config: Extend colors to match Discord's palette (Blurple, Dark, etc.)
Font: Use 'Inter' or 'Roboto' as a fallback for a clean, modern look.
Polling: Use `refetchInterval` in React Query for real-time updates on the dashboard.
